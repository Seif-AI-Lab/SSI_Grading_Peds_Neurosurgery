# Streamlit Community Cloud: deploy this app with Python 3.12.
# The serialized model requires the pinned scientific-Python versions below.
# Pinned to the environment that produced grading_model.joblib.
#
# These pins are not optional. The artefact was fitted with scikit-learn 1.6.1
# under NumPy 2.x, and both bounds were verified by testing:
#   - scikit-learn 1.8 unpickles the file but SimpleImputer.transform then
#     fails with AttributeError: '_fill_dtype'
#   - NumPy 1.26 fails at load time with
#     ValueError: PCG64 is not a known BitGenerator module
# Leaving either unpinned is the usual reason an app that works locally breaks
# once deployed.
streamlit==1.40.2
scikit-learn==1.6.1
numpy==2.0.2
scipy==1.14.1
pandas==2.2.3
joblib==1.4.2
